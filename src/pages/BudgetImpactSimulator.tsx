import { formatCurrency } from '../shared/utils/formatters';

export default function BudgetImpactSimulator() {
  return (
    <div className="min-h-screen bg-gray-50 py-12">
      {/* ... existing code ... */}
    </div>
  );
} 